<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand" href="/products">Tienda Online</a>
        <div class="collapse navbar-collapse">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="/products">Productos</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/tickets">Mis Tickets</a>
                </li>
            </ul>
            <div class="d-flex align-items-center">
                {{#if user.name}}
                <span class="text-light me-3">Hola, {{user.name}}!</span>
                {{/if}}
                <a href="/cart" class="btn btn-outline-light me-2">
                    Carrito 🛒
                </a>
                <a href="#" onclick="logout()" class="btn btn-outline-secondary">
                    Salir
                </a>
            </div>
        </div>
    </div>
</nav>